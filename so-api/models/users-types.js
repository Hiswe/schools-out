'use strict'

const USER = `user`
// const TEACHER = `teacher`
const ADMIN = `admin`
const list = Object.freeze([
  USER,
  // TEACHER,
  ADMIN,
])

module.exports = {
  USER,
  // TEACHER,
  ADMIN,
  list,
}
